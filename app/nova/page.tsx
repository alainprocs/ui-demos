import type { Metadata } from "next";
import NovaHome from "@/demos/nova/page";

export const metadata: Metadata = {
  title: "Nova — Infrastructure at the Speed of Light",
  description:
    "SaaS landing page for a real-time infrastructure monitoring platform — electric cyan on near-black, live metric streams, and cinematic data visualizations.",
};

export default NovaHome;
