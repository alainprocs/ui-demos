import type { Metadata } from "next";
import DriftwaveHome from "@/demos/driftwave/page";

export const metadata: Metadata = {
  title: "Driftwave — Async Video Collaboration for Design Teams",
  description:
    "Driftwave replaces endless meetings with rich async video threads. Share context, collect feedback, and ship faster — all without scheduling a call.",
};

export default DriftwaveHome;
