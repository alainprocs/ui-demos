import type { Metadata } from "next"
import AtlasHome from "@/demos/atlas/page"

export const metadata: Metadata = {
  title: "Atlas — Global Edge Network",
  description: "Deliver your app from 310 edge locations worldwide. Sub-8ms global latency, zero config, infinite scale.",
}

export default AtlasHome
