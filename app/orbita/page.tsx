import type { Metadata } from "next";
import OrbitaHome from "@/demos/orbita/page";

export const metadata: Metadata = {
  title: "Orbita — AI Infrastructure Cost Intelligence",
  description:
    "Orbita analyzes your entire AWS/GCP/Azure spend in real time and eliminates waste automatically. Zero config. Instant savings.",
};

export default OrbitaHome;
